# Copy to supabase/.env — read by `supabase start` for config.toml env() substitution.
# Only needed if you enable Google sign-in locally ([auth.external.google] in config.toml).
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID=
SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET=
